matin= float(input('saisir une valeur de pression pour le matin : '))
midi= float(input('saisir une valeur de pression pour le midi : '))
soir= float(input('saisir une valeur de pression pour le soir : '))
resultat = (matin + midi + soir)/3
print(f'la moyenne de pression est : {resultat} mmHg')

